[package]
name = "heap_tests"
version = "0.1.0"
edition.workspace = true

[dependencies]
vm = { path = "../vm" }
dummy_heap = { path = "../collectors/dummy_heap" }
mark_sweep = { path = "../collectors/mark_sweep" }
