[package]
name = "vm_core"
version = "0.1.0"
edition.workspace = true

[dependencies]
heap_api = { path = "../heap_api" }
bytecode = { path = "../bytecode" }
trace = { path = "../trace" }

[features]
stress-minor-gc = []
