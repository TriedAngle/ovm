[package]
name = "mark_sweep"
version = "0.1.0"
edition.workspace = true

[dependencies]
heap_api = { path = "../../heap_api" }
heap_utils = { path = "../../heap_utils" }

[features]
# minor-GC cycle tracing (OVM_GCSTAT env var gates the printing)
gc-stats = []
